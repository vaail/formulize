import * as Formulize from './formulize';

describe('integration test: Formulize module', () => {
    describe('test method: Formulize.get()', () => {
        it('should return dot-separated string with ', () => {
            Formulize.getVersion();
        });
    });
});
